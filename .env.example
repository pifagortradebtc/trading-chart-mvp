# Локально кеш пишется в ./.cache-disk (см. .gitignore).
# На Render с persistent disk укажите точку монтирования:
PERSISTENT_DISK_ROOT=/data

# Отключить запросы к /api/ohlcv и использовать только браузер + Binance + IndexedDB:
# NEXT_PUBLIC_SERVER_DISK_CACHE=0
